<!DOCTYPE html>
<html lang="pt-br">
<div>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - UNINOVE</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: url('/mnt/data/imagem.png') no-repeat center center fixed;
            background-size: cover;
            font-family: Arial, sans-serif;
        }

        .login-card {
            width: 380px;
            border-radius: 10px;
            background: #fff;
            box-shadow: 0 4px 25px rgba(0,0,0,0.15);
        }

        .login-header img {
            width: 160px;
        }

        .toggle-buttons button {
            width: 120px;
        }

        .btn-primary-custom {
            background-color: #2196f3;
            border: none;
        }

        .btn-primary-custom:hover {
            background-color: #0d83e8;
        }

        a {
            font-size: 14px;
        }
    </style>
</div>
<div>

<div class="d-flex justify-content-center align-items-center vh-100">
    
    <div class="login-card p-4">

        <!-- Logo -->
        <div class="login-header text-center mb-3">
            <img src="https://upload.wikimedia.org/wikipedia/commons/0/0e/Uninove_logo.png" alt="UNINOVE">
        </div>

        <p class="text-center">Realizar o acesso por:</p>

        <!-- Botões RA / CPF -->
        <div class="d-flex justify-content-center mb-3 toggle-buttons">
            <button class="btn btn-primary me-2">RA</button>
            <button class="btn btn-secondary">CPF</button>
        </div>

        <!-- Campo RA -->
        <div class="mb-3">
            <label class="form-label">Digite seu RA</label>
            <div class="input-group">
                <span class="input-group-text">
                    <i class="bi bi-person"></i>
                </span>
                <input type="text" class="form-control" placeholder="RA">
            </div>
        </div>

        <!-- Campo Senha -->
        <div class="mb-3">
            <label class="form-label">Digite sua senha</label>
            <div class="input-group">
                <span class="input-group-text">
                    <i class="bi bi-lock"></i>
                </span>
                <input type="password" class="form-control" placeholder="Senha">
            </div>
        </div>

        <!-- Link -->
        <div class="text-center mb-3">
            <a href="#">Esqueci minha senha / Primeiro acesso</a>
        </div>

        <!-- Botão Entrar -->
        <button class="btn btn-primary-custom w-100 py-2">ENTRAR</button>
    </div>

</div>

<!-- Bootstrap Icons -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

</div
</html>
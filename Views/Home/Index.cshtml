<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LogTec | Início</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Icons (para emojis mais consistentes caso queira usar ícones reais depois) -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

  <style>
    body {
    font-family: Arial, sans-serif;
    background-image: url('https://img.freepik.com/vetores-premium/um-grafico-de-pessoas-subindo-em-um-grafico_9206-8921.jpg'); /* imagem enviada */
    background-size: cover;        /* preenche toda a tela */
    background-repeat: no-repeat;  /* não repete */
    background-position: center;   /* centraliza */
    background-attachment: fixed;  /* fixa o fundo ao rolar */
    }

    /* Seção do topo */
    .hero {
      background: #0d6832;
      border-radius: 15px;
      padding: 45px 20px;
      box-shadow: 0px 2px 8px rgba(0, 0, 0, 0.25);
      margin: 30px;
      text-align: center;
      color: #ffffff;
    }

    .hero h1 {
      font-size: 2.6rem;
      font-weight: 900;
      color: #ffffff;
    }

    /* Cards */
    .card-custom {
      border-radius: 12px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.15);
      transition: transform .2s ease;
    }

    .card-custom:hover {
      transform: translateY(-5px);
    }

    .icon {
      font-size: 30px;
      margin-right: 8px;
    }

    /* Botões */
    .btn-primary {
      background: #1a62ff;
      border: none;
    }

    .btn-success {
      background: #14a027;
      border: none;
    }

    .btn-danger {
      background: #d32a2a;
      border: none;
    }

    /* Rodapé */
    footer {
      text-align: center;
      font-size: 14px;
      margin-top: 50px;
      padding: 20px 0;
      color: #555;
    }
  </style>

</head>

<body>

  <!-- Header -->
  <header class="hero">
    <h1>Bem-vindo ao Sistema Logtec!</h1>
    <p class="mt-3">Gerencie seu sistema de forma eficiente e rápida.</p>
    <hr class="w-25 mx-auto mt-3" style="border: 1px solid #3d4e6b;">
  </header>

  <!-- Cards -->
  <div class="container mb-5">
    <div class="row justify-content-center g-4">

      <!-- Cliente -->
      <div class="col-md-4">
        <div class="card-custom p-4 bg-white">
          <h4 class="d-flex align-items-center">
            <span class="icon">🧑</span> Cliente
          </h4>
          <p class="mt-3">Gerencie o cadastro de clientes, visualize perfis e mantenha as informações atualizadas.</p>
          <a asp-controller="Cliente" asp-action="Index" class="btn btn-primary w-100 mt-3">
            Acessar
          </a>
        </div>
      </div>

      <!-- Pedido -->
      <div class="col-md-4">
        <div class="card-custom p-4 bg-white">
          <h4 class="d-flex align-items-center">
            <span class="icon">📦</span> Produto
          </h4>
          <p class="mt-3">Gerencie os pedidos realizados, acompanhe o status de entrega ou faça novas consultas.</p>
          <a asp-controller="Produto" asp-action="Index" class="btn btn-success w-100 mt-3">
            Acessar
          </a>
        </div>
      </div>

      <!-- Produto -->
      <div class="col-md-4">
        <div class="card-custom p-4 bg-white">
          <h4 class="d-flex align-items-center">
            <span class="icon">📄✏️</span> Pedido
          </h4>
          <p class="mt-3">Gerencie o estoque dos produtos, realize cadastros e controle a disponibilidade em tempo real.
          </p>
          <a asp-controller="Pedido" asp-action="Index" class="btn btn-danger w-100 mt-3">
            Acessar
          </a>
        </div>
      </div>

    </div>
  </div>

  <!-- Rodapé -->
  <hr class="mx-auto w-75">
  <footer>
    © 2025 Logtec Systems. Todos os direitos reservados.
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>